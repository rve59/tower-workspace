FROM node:20-slim

WORKDIR /app

# Copy the whole workspace to ensure context is correct
COPY . .

# Navigate to frontend and install dependencies
WORKDIR /app/tower_core/apps/frontend
RUN npm install

EXPOSE 9041

CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "9041"]
